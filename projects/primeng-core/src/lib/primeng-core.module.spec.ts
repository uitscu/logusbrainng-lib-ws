import { PrimengCoreModule } from './primeng-core.module';

describe('PrimengCoreModule', () => {
  it('should expose the library module', () => {
    expect(PrimengCoreModule).toBeDefined();
  });
});
